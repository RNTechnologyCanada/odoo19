[options]
; IMPORTANT for Render / reverse proxy
proxy_mode = True

; Run single-process on Render (avoids longpolling port 8072 issue)
workers = 0
max_cron_threads = 1

; Optional logging
log_level = info
